{
  "server_url": {
    "description": "Suwayomi-Server 地址",
    "type": "string",
    "default": "http://localhost:4567",
    "hint": "例如 http://192.168.1.100:4567。请勿随意更换suwayomi-server实例，否则会导致ID混乱。参阅https://github.com/FFFold/astrbot_plugin_suwayomi_server/blob/main/docs/setup.md"
  },
  "auth_mode": {
    "description": "认证模式",
    "type": "string",
    "default": "none",
    "options": ["none", "basic", "jwt"],
    "labels": ["无认证", "Basic 认证", "JWT 认证"]
  },
  "username": {
    "description": "认证用户名",
    "type": "string",
    "default": ""
  },
  "password": {
    "description": "认证密码",
    "type": "string",
    "default": ""
  },
  "check_interval": {
    "description": "更新检查间隔（分钟）",
    "type": "int",
    "default": 60,
    "hint": "每隔多久检查一次漫画更新"
  },
  "max_pages": {
    "description": "单次阅读最大发送页数",
    "type": "int",
    "default": 30
  },
  "send_mode": {
    "description": "图片发送模式",
    "type": "string",
    "default": "image",
    "options": ["image", "forward"],
    "labels": ["直接发图", "合并转发"]
  },
  "image_fetch_mode": {
    "description": "图片获取方式",
    "type": "string",
    "default": "url",
    "options": ["url", "download"],
    "labels": ["URL 直接引用", "先下载到本地"],
    "hint": "URL 方式更快但网络不稳定时容易失败；下载方式更可靠但占用临时磁盘空间"
  },
  "download_concurrency": {
    "description": "并行下载数",
    "type": "int",
    "default": 6,
    "hint": "同时下载的图片数量（仅下载模式生效）"
  },
  "download_retries": {
    "description": "下载重试次数",
    "type": "int",
    "default": 3,
    "hint": "单张图片下载失败后的重试次数（指数退避）"
  },
  "default_source_id": {
    "description": "默认搜索源 ID",
    "type": "int",
    "default": 0,
    "hint": "0 表示搜索所有已安装源"
  },
  "chapter_cache_hours": {
    "description": "章节缓存时间（小时）",
    "type": "int",
    "default": 6,
    "min": -1,
    "hint": "超过此时间后自动从源刷新章节数据，0 表示永不自动刷新，-1 表示每次都强制刷新"
  },
  "download_format": {
    "description": "默认下载打包格式",
    "type": "string",
    "default": "zip",
    "options": ["zip", "pdf", "cbz"],
    "labels": ["ZIP 压缩包", "PDF 文档", "CBZ 漫画"]
  },
  "temp_dir": {
    "description": "临时文件目录",
    "type": "string",
    "default": "",
    "hint": "留空使用系统默认临时目录。Docker 环境请设置为 AstrBot 和聊天平台容器共享的目录，例如 /AstrBot/data/temp"
  },
  "auto_push_mode": {
    "description": "自动推送模式",
    "type": "string",
    "default": "image",
    "options": ["image", "file"],
    "labels": ["图片（复用阅读）", "文件（复用下载）"],
    "hint": "发现更新时自动推送的模式：图片直接发到聊天，文件发送 ZIP/PDF/CBZ 包"
  },
  "enable_ai_tools": {
    "description": "启用 AI 漫画工具",
    "type": "bool",
    "default": true,
    "hint": "允许 AstrBot Agent 使用自然语言搜索漫画、查询章节；关闭后保留全部 /漫画 命令"
  },
  "allow_ai_send": {
    "description": "允许 AI 直接发送漫画",
    "type": "bool",
    "default": true,
    "hint": "仅当用户明确要求阅读或发送漫画时生效；默认发送 PDF，搜索和询问不会主动发送文件或图片"
  },
  "ai_max_sources": {
    "description": "AI 单次搜索最多漫画源数",
    "type": "int",
    "default": 5,
    "min": 1,
    "max": 10,
    "hint": "限制 Agent 单次搜索的漫画源数量，避免响应过慢"
  },
  "ai_results_per_source": {
    "description": "AI 每个源最多候选数",
    "type": "int",
    "default": 5,
    "min": 1,
    "max": 20
  },
  "ai_tool_timeout_sec": {
    "description": "AI Tool 超时（秒）",
    "type": "int",
    "default": 60,
    "min": 10,
    "max": 300,
    "hint": "搜索、章节查询和阅读发送的单次 Tool 超时"
  }
}
